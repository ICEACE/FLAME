/**
 * \file  centralbank_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of centralbank agent memory variable. */
#define ID (current_xmachine_centralbank->id)
/** \def DAY_OF_MONTH_TO_ACT
 * \brief Direct access to day_of_month_to_act of centralbank agent memory variable. */
#define DAY_OF_MONTH_TO_ACT (current_xmachine_centralbank->day_of_month_to_act)
/** \def UNEMPLOYMENT_RATE
 * \brief Direct access to unemployment_rate of centralbank agent memory variable. */
#define UNEMPLOYMENT_RATE (current_xmachine_centralbank->unemployment_rate)
/** \def INFLATION_RATE
 * \brief Direct access to inflation_rate of centralbank agent memory variable. */
#define INFLATION_RATE (current_xmachine_centralbank->inflation_rate)
/** \def CONSUMPTION_GOODS_PRICES
 * \brief Direct access to consumption_goods_prices of centralbank agent memory variable. */
#define CONSUMPTION_GOODS_PRICES (current_xmachine_centralbank->consumption_goods_prices)
/** \def IT_NO
 * \brief Direct access to it_no of centralbank agent memory variable. */
#define IT_NO (current_xmachine_centralbank->it_no)
/** \def DAY_OF_WEEK_TO_ACT
 * \brief Direct access to day_of_week_to_act of centralbank agent memory variable. */
#define DAY_OF_WEEK_TO_ACT (current_xmachine_centralbank->day_of_week_to_act)
/** \def GOODS
 * \brief Direct access to goods of centralbank agent memory variable. */
#define GOODS (current_xmachine_centralbank->goods)
/** \def WEEKLY_PRICE_AVERAGES
 * \brief Direct access to weekly_price_averages of centralbank agent memory variable. */
#define WEEKLY_PRICE_AVERAGES (current_xmachine_centralbank->weekly_price_averages)
/** \def DAY_OF_MONTH_WAGES_PAID
 * \brief Direct access to day_of_month_wages_paid of centralbank agent memory variable. */
#define DAY_OF_MONTH_WAGES_PAID (current_xmachine_centralbank->day_of_month_wages_paid)
/** \def INTEREST_RATE
 * \brief Direct access to interest_rate of centralbank agent memory variable. */
#define INTEREST_RATE (current_xmachine_centralbank->interest_rate)
/** \def LIQUIDITY
 * \brief Direct access to liquidity of centralbank agent memory variable. */
#define LIQUIDITY (current_xmachine_centralbank->liquidity)
/** \def LOANS_BANKS
 * \brief Direct access to loans_banks of centralbank agent memory variable. */
#define LOANS_BANKS (current_xmachine_centralbank->loans_banks)
/** \def LOANS_GOVERNMENT
 * \brief Direct access to loans_government of centralbank agent memory variable. */
#define LOANS_GOVERNMENT (current_xmachine_centralbank->loans_government)
/** \def FIAT_MONEY
 * \brief Direct access to fiat_money of centralbank agent memory variable. */
#define FIAT_MONEY (current_xmachine_centralbank->fiat_money)
/** \def EQUITY
 * \brief Direct access to equity of centralbank agent memory variable. */
#define EQUITY (current_xmachine_centralbank->equity)
/** \def LIQUIDITY_BANKS
 * \brief Direct access to liquidity_banks of centralbank agent memory variable. */
#define LIQUIDITY_BANKS (current_xmachine_centralbank->liquidity_banks)
/** \def LIQUIDITY_GOVERNMENT
 * \brief Direct access to liquidity_government of centralbank agent memory variable. */
#define LIQUIDITY_GOVERNMENT (current_xmachine_centralbank->liquidity_government)
/** \def LIQUIDITY_EQUITYFUND
 * \brief Direct access to liquidity_equityfund of centralbank agent memory variable. */
#define LIQUIDITY_EQUITYFUND (current_xmachine_centralbank->liquidity_equityfund)
/** \def TOTAL_ASSETS
 * \brief Direct access to total_assets of centralbank agent memory variable. */
#define TOTAL_ASSETS (current_xmachine_centralbank->total_assets)
/** \def TOTAL_WRITEOFFS
 * \brief Direct access to total_writeoffs of centralbank agent memory variable. */
#define TOTAL_WRITEOFFS (current_xmachine_centralbank->total_writeoffs)
/** \def INTERESTS_ACCRUED
 * \brief Direct access to interests_accrued of centralbank agent memory variable. */
#define INTERESTS_ACCRUED (current_xmachine_centralbank->interests_accrued)
/** \def REVENUES
 * \brief Direct access to revenues of centralbank agent memory variable. */
#define REVENUES (current_xmachine_centralbank->revenues)
/** \def NET_EARNINGS
 * \brief Direct access to net_earnings of centralbank agent memory variable. */
#define NET_EARNINGS (current_xmachine_centralbank->net_earnings)
/** \def TOTAL_COSTS
 * \brief Direct access to total_costs of centralbank agent memory variable. */
#define TOTAL_COSTS (current_xmachine_centralbank->total_costs)
/** \def HOUSES
 * \brief Direct access to houses of centralbank agent memory variable. */
#define HOUSES (current_xmachine_centralbank->houses)
