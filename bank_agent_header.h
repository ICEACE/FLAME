/**
 * \file  bank_agent_header.h
 * \brief Header for agent type memory access.
 */

/** \def ID
 * \brief Direct access to id of bank agent memory variable. */
#define ID (current_xmachine_bank->id)
/** \def DAY_OF_MONTH_TO_ACT
 * \brief Direct access to day_of_month_to_act of bank agent memory variable. */
#define DAY_OF_MONTH_TO_ACT (current_xmachine_bank->day_of_month_to_act)
/** \def LOANS
 * \brief Direct access to loans of bank agent memory variable. */
#define LOANS (current_xmachine_bank->loans)
/** \def MORTGAGES
 * \brief Direct access to mortgages of bank agent memory variable. */
#define MORTGAGES (current_xmachine_bank->mortgages)
/** \def DEPOSITS
 * \brief Direct access to deposits of bank agent memory variable. */
#define DEPOSITS (current_xmachine_bank->deposits)
/** \def DEBT
 * \brief Direct access to debt of bank agent memory variable. */
#define DEBT (current_xmachine_bank->debt)
/** \def EQUITY
 * \brief Direct access to equity of bank agent memory variable. */
#define EQUITY (current_xmachine_bank->equity)
/** \def LIQUIDITY
 * \brief Direct access to liquidity of bank agent memory variable. */
#define LIQUIDITY (current_xmachine_bank->liquidity)
/** \def TOTAL_WRITEOFFS
 * \brief Direct access to total_writeoffs of bank agent memory variable. */
#define TOTAL_WRITEOFFS (current_xmachine_bank->total_writeoffs)
/** \def INTERESTS_ACCRUED
 * \brief Direct access to interests_accrued of bank agent memory variable. */
#define INTERESTS_ACCRUED (current_xmachine_bank->interests_accrued)
/** \def INTERESTS_PAID
 * \brief Direct access to interests_paid of bank agent memory variable. */
#define INTERESTS_PAID (current_xmachine_bank->interests_paid)
/** \def DIVIDENDS_PAID
 * \brief Direct access to dividends_paid of bank agent memory variable. */
#define DIVIDENDS_PAID (current_xmachine_bank->dividends_paid)
